// TacoBoard 2.0 configuration
// Fill these in after creating your Supabase project.
// Leaving them blank runs TacoBoard in local preview mode (great for testing UI, not shareable).
window.TACOBOARD_CONFIG = {
  SUPABASE_URL: '',
  SUPABASE_PUBLISHABLE_KEY: '',
  // Optional. TacoBoard works without it; users can still upload images/GIFs or paste GIF URLs.
  GIPHY_API_KEY: ''
};
